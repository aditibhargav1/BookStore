CREATE TABLE books (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title VARCHAR2(255),
  author VARCHAR2(255),
  price NUMBER,
  description CLOB,
  image_url VARCHAR2(500),
  genre VARCHAR2(100)
);

COMMIT;
